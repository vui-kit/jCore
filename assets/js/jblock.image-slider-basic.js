/*!
 * jBlock.image-slider-basic
 * v1.0.0
 * https://jlomoglio.github.io/jcore
 * https://github.com/jlomoglio/jCore
 * copyright Joe LoMoglio 2010 - 2018
*/

(function($) {
    $.fn.imageSliderBasic = function(options) {
        // Establish our default settings
        var settings = $.extend({
            interval        : 10000,
            slideWidth      : '100%',
            slideHeight     : '600px',
            callback        : null
        }, options);

        // Establish base stylesheet
        var styles = {

        }
    }

}(jQuery));
